{
  "name": "scallenews-learning",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "description": "Repositório de estudos dedicado ao desenvolvimento do projeto ScalleNews.",
  "main": "index.js",
  "scripts": {
    "dev": "npm run services:up && npm run services:wait:database && npm run migrations:up && next dev",
    "test": "npm run services:up && npm run check:env && concurrently -n next,jest --hide next -k -s command-jest \"next dev\" \"jest --runInBand --verbose\"",
    "test:ci": "NODE_ENV=test jest --runInBand --verbose",
    "test:watch": "jest --watchAll --runInBand",
    "build": "next build",
    "start": "next start",
    "services:up": "docker compose -f infra/compose.yaml up -d",
    "services:stop": "docker compose -f infra/compose.yaml stop",
    "services:down": "docker compose -f infra/compose.yaml down -v",
    "services:wait:database": "node infra/scripts/wait-for-postgres.js",
    "migrations:create": "node-pg-migrate -m infra/migrations create",
    "migrations:up": "node-pg-migrate -m infra/migrations --envPath .env.development up",
    "prepare": "husky",
    "lint:prettier:check": "prettier --check .",
    "lint:prettier:fix": "prettier --write .",
    "lint:eslint:check": "eslint .",
    "lint:eslint:fix": "eslint . --fix",
    "check:env": "node -e \"const { execSync } = require('child_process'); try { const status = execSync('git status --porcelain').toString(); if (status.includes('.env') && !status.includes('.env.example')) { console.error('❌ ALERTA: Arquivo .env detectado no Git! Remova antes de commitar.'); process.exit(1); } else { console.log('✅ Tudo limpo! Nenhum .env exposto para o GitHub.'); } } catch (e) { console.log('⚠️ Git sem arquivos para checar.'); }\"",
    "commit": "cz"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "async-retry": "1.3.3",
    "dotenv": "17.4.2",
    "dotenv-expand": "13.0.0",
    "next": "^16.2.9",
    "node-pg-migrate": "8.0.4",
    "pg": "8.22.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@commitlint/cli": "21.1.0",
    "@commitlint/config-conventional": "21.1.0",
    "@eslint/js": "10.0.1",
    "commitizen": "4.3.2",
    "concurrently": "10.0.3",
    "cz-conventional-changelog": "3.3.0",
    "eslint": "10.6.0",
    "eslint-config-next": "16.2.9",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-jest": "29.15.3",
    "globals": "17.7.0",
    "husky": "9.1.7",
    "jest": "^30.4.2",
    "prettier": "3.9.1"
  },
  "overrides": {
    "js-yaml": "^4.1.0",
    "postcss": "^8.5.10"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
